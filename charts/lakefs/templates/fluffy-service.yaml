{{- if (.Values.fluffy).enabled }}
{{- if .Values.fluffy.sso.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: fluffy-sso
  labels:
    {{- include "fluffy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.fluffy.sso.service.type }}
  ports:
    - port: {{ .Values.fluffy.sso.service.port }}
      targetPort: http-sso
      protocol: TCP
      name: http-sso
  selector:
    {{- include "fluffy.selectorLabels" . | nindent 4 }}
{{- end }}
---
{{- if .Values.fluffy.rbac.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: fluffy-rbac
  labels:
    {{- include "fluffy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.fluffy.rbac.service.type }}
  ports:
    - port: {{ .Values.fluffy.rbac.service.port }}
      targetPort: http-rbac
      protocol: TCP
      name: http-rbac
  selector:
    {{- include "fluffy.selectorLabels" . | nindent 4 }}
{{- end }}
{{- end}}