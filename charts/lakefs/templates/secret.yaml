{{- if .Values.lakefsConfig }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "lakefs.fullname" . }}
  labels:
    {{- include "lakefs.labels" . | nindent 4 }}
type: Opaque
data:
  database_connection_string: {{ .Values.databaseConnectionString | default "" | b64enc }}
  auth_encrypt_secret_key: {{ .Values.authEncryptSecretKey | default "" | b64enc }}
{{- end}}
